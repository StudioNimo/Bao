name: ci

# main への PR では必ず実行（ブランチ保護の「必須ステータス」に指定しやすい）
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install deps (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential sqlite3 libsqlite3-dev

      - name: Build
        run: make RELEASE=1

      - name: Test
        run: make test

  # マトリックス全体が成功したときだけ成功する 1 本のチェック（ブランチ保護でこれ 1 つを必須にできる）
  ci-success:
    name: ci (required)
    needs: test
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Require all matrix jobs to succeed
        run: |
          if [ "${{ needs.test.result }}" != "success" ]; then
            echo "One or more test jobs failed or were cancelled."
            exit 1
          fi
          echo "All test jobs succeeded."
